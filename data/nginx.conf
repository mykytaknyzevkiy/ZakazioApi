server {
    listen 80;
    listen [::]:80;
    server_name zakazy.online;

    location ~ /.well-known {
        allow all;
    }

    location ~ /\.well-known/acme-challenge {
    	root /site/sdk;
    }

    root /site/sdk;
    index index.html;
}