server {
    listen 80;
    listen [::]:80;
    server_name zakazy.online;
    location ~ /.well-known/acme-challenge {
        allow all;
        root /usr/share/nginx/html;
    }
    root /site/sdk;
    index index.html;
}