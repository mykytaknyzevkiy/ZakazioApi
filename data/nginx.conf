server {
    listen 80;
    listen [::]:80;
    server_name zakazy.online;
    location ~ /\.well-known/acme-challenge/ {
      allow all;
      try_files $uri =404;
      break;
    }
    root /site/sdk;
    index index.html;
}