server {
    listen 80;
    listen [::]:80;
    server_name zakazy.online;

    location ~ /.well-known/acme-challenge {
      allow all;
      root /site/sdk;
    }

    root /site/sdk;
    index index.html;
}